#Project has two sides
#Side one working code (raw)
#Side two stuckture
#
#To run code Playwright and Nodejs should be installed:
#Clone directory and open cloned dir wih terminal
#Run playwright command in terminal: npx playwright test --ui
